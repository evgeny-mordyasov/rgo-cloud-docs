server:
  port: 8091

spring:
  main:
    banner-mode: off

module-properties:
  auth:
    cookie:
      authCookieName: TEST_TOKEN
      path: /
      expirationHours: 1
      secret: test

jasypt:
  encryptor:
    property:
      prefix: STUB(
      suffix: )